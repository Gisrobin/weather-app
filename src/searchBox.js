import React from '.react';

const searchBox =({ searchField,searchChamge }) => (
    <div>
        <input
        className =""
        type=""
        placeholder="search weather"
        />
    </div>

);